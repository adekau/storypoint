export interface IUser {
    roomId: string;
    userId: string;
    nickname: string;
    vote?: number;
}

export interface IUserDetail {
    roomId: string;
    userId: string;
    nickname: string;
    vote?: number;
}